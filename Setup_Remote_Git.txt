cd c:/Users/LBRY-SVC-Patron/Documents/Python
git config --global user.name "Behzad Mortazavi"
git config --gloval user.email "ubc email address"
git config --global init.defaultBranch main
git config --global --list
git remote add origin https://github.com/bmortaz01/book.git
git remote -v

1- To setup remote Github repository for the first time.
    git init
    git checkout -b main
    git add .
    git commit -m "First Commit"
    git push --force origin main
2- To setup local git repository for the first time.
    git init
    git checkout -b main
    git pull --force origin main
